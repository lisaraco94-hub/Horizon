// Prisma 7 requires an adapter (e.g., @prisma/pg-adapter).
// Database client will be configured when PostgreSQL is available.
// For MVP, the app uses mock data from constants.ts.
//
// Example setup with adapter:
// import { PrismaClient } from "../generated/prisma/client";
// import { PrismaPg } from "@prisma/pg-adapter";
//
// const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL });
// export const prisma = new PrismaClient({ adapter });

export {};
